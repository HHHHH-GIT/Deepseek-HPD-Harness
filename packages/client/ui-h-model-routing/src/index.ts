/**
 * H model-routing plan dock, node half. This pure browser plugin appears in
 * the host roster while `@deepseek-ai/dsh-h-model-routing` owns the durable
 * plan state and its session projection.
 */

/** Host plugin body — no host-side behavior for this presentation plugin. */
export function apply(): void {}
